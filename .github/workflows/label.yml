name: Label PR

on:
  pull_request_target:
    types: [opened, edited]

jobs:
  label:
    name: Label Pull Request
    runs-on: ubuntu-latest
    steps:
      - uses: actions/labeler@v5
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"
